name: human
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.9
    - flask
    - flask-login
    - flask-cors
    - gunicorn
    - pandas
    - pip
    - pillow
    - pytables
    - python-dotenv
    - requests-toolbelt
    - tqdm
    - transitions
    - ruamel.yaml=0.17.20
    - bcrypt
